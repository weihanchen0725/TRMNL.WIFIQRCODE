{
  "name": "trmnal-wifi",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://weihanchen0725.github.io/TRMNL.WIFIQRCODE",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.2"
  }
}
